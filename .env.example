# Tool correctness and exact match work without a judge.
# For LLM metrics, configure a Chat Completions compatible JSON-mode model.
EVAL_JUDGE_MODEL=
EVAL_JUDGE_API_KEY=
# Leave empty for the default OpenAI endpoint, or set a compatible endpoint.
EVAL_JUDGE_BASE_URL=
EVAL_MAX_CONCURRENT=4
EVAL_METRIC_TIMEOUT_SECONDS=120
